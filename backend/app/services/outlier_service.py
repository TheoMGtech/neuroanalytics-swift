# Outlier and representative amples detector
import pandas as pd

def detect_outliers(df: pd.DataFrame) -> list:
    # Identify store outliers (z-score, IQR, and statistical representation)
    return []
